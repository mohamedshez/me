# Optional. Anonymous public GitHub reads work without credentials.
# Use a read-only fine-grained token with public metadata access for a higher rate limit.
GITHUB_TOKEN=
# Change only when moving the canonical production domain.
SITE_URL=https://www.shez.app
